# DeepSeek Harness — environment variables.
# Copy to .env (git-ignored) and fill in real values; never commit credentials.
# Key policy: docs/testing.md.

# Required by real-API tests and demos; keyless suites self-skip without it.
DEEPSEEK_API_KEY=

# Optional override for the DeepSeek-compatible API endpoint.
# DEEPSEEK_BASE_URL=
